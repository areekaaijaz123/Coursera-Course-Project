# Coursera-Course-Project

1. Reading data from files

i) Reading features
ii) Reading training data, training activity and training subject
iii) Reading testing data, testing activity and testing subject
iv) Making data.frame of training set
v) Making data.frame of testing set

2. Merged the training and the test sets to create one data set

3. Uses descriptive activity names to name the activities in the data set

4. Appropriately labels the data set with descriptive variable names

5. From the data set in step 4, creates a second, independent tidy data set with the average of each variable for each activity and each subject

6. Writing to file 'Tidy Data. txt'
